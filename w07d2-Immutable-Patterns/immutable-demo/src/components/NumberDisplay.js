import React from 'react';

// building a component
// make a file called the name of the component ____.js
// import React from 'react' <-- older ver, new ver does this automatically

// make a function and call it the same name as the component file

function NumberDisplay(props) {

    // must return some JSX 
    return (
        <div>
            Number Display!!
        </div>
    )
}


// export default 

export default NumberDisplay;